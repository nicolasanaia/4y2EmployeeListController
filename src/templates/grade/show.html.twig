{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>StudentName</th>
                <td>{{ grade.studentName }}</td>
            </tr>
            <tr>
                <th>StudentGrade</th>
                <td>{{ grade.studentGrade }}</td>
            </tr>
            <tr>
                <th>TeacherName</th>
                <td>{{ grade.teacherName }}</td>
            </tr>
            <tr>
                <th>TestNumber</th>
                <td>{{ grade.testNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grade_index') }}">back to list</a>

    <a href="{{ path('grade_edit', {'id': grade.id}) }}">edit</a>

    {{ include('grade/_delete_form.html.twig') }}
{% endblock %}
