<!DOCTYPE html>
<html>
    <head>
        <link rel="icon" href="/Assets/img/4Y2-fundo-escuro.svg" sizes="32x32">
        <div class="dropdown">
            <button class="dropbtn">Students</button>
            <div class="dropdown-content">
                <a class="button" href="/students">Students</a>
                <a class="button" href="/satisfaction">Satisfaction</a>
            </div>
        </div>
        <div class="dropdown">
            <button class="dropbtn">Adm</button>
            <div class="dropdown-content">
                <a class="button" href="/grade">Grade</a>
                <a class="button" href="/unit">Unit</a>
                <a class="button" href="/students/class">Classes</a>
            </div>
        </div>
        {% if app.user %}
            <div>
                <p>You're logged in as {{ app.user }} <a href="{{ path('app_logout') }}">Exit</a></p>
            </div>
        {% endif %}

        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
        <style>
            body{
                background-color: rgb(21, 21, 21);
                color: #ffffff;
                text-align: center;
            }
            a{
                color: #5889b1;
            }
            a:hover{
                color: #5a4e4e;
            }
            table{
                width: 100%;
                margin-top: 100px;
                margin-left: auto;
                margin-right: auto;
                border: 2px solid #ffffff;
                box-sizing: border-box;
                border-radius: 10px;
                }

            thead{
                color: white;
            }
            td{
                color: white;
            }

            .dropbtn {
                background-color: #ececec;
                color: #11172a;
                padding: 12px;
                font-size: 14px;
                border: none;
                border-radius: 5px;
            }

            .dropdown {
                position: relative;
                display: inline-block;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                background-color: #f1f1f1;
                min-width: 160px;
                box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
                z-index: 1;
                border-radius: 5px;
            }

            .dropdown-content a {
                color: #11172a;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
                border-radius: 5px;
            }

            .dropdown-content a:hover {background-color: rgb(182, 195, 222);}

            .dropdown:hover .dropdown-content {display: block;}

            .dropdown:hover .dropbtn {background-color: rgb(182, 195, 222);}

        </style>
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
