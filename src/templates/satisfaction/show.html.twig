{% extends 'base.html.twig' %}

{% block title %}Satisfaction{% endblock %}

{% block body %}
    <h1>Satisfaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ satisfaction.id }}</td>
            </tr>
            <tr>
                <th>StudentName</th>
                <td>{{ satisfaction.studentName }}</td>
            </tr>
            <tr>
                <th>ClassRated</th>
                <td>{{ satisfaction.classRated }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ satisfaction.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('satisfaction_index') }}">back to list</a>

    <a href="{{ path('satisfaction_edit', {'id': satisfaction.id}) }}">edit</a>

    {{ include('satisfaction/_delete_form.html.twig') }}
{% endblock %}
