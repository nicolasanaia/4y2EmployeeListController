<form method="post" action="{{ path('students_class_delete', {'id': students_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ students_class.id) }}">
    <button class="btn">Delete</button>
</form>
