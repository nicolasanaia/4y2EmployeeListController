{% extends 'base.html.twig' %}

{% block title %}StudentsClass{% endblock %}

{% block body %}
    <h1>StudentsClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ students_class.id }}</td>
            </tr>
            <tr>
                <th>WeekDay</th>
                <td>{{ students_class.weekDay }}</td>
            </tr>
            <tr>
                <th>ClassSchedule</th>
                <td>{{ students_class.classSchedule }}</td>
            </tr>
        </tbody>
    </table>

    <hr>
    <h2>Student's Classes</h2>
    {% for studentsClass in students.studentsClass %}
        <p>{{ studentsClass }}</p>
    {% endfor %}

    <a href="{{ path('students_class_index') }}">back to list</a>

    <a href="{{ path('students_class_edit', {'id': students_class.id}) }}">edit</a>

    {{ include('students_class/_delete_form.html.twig') }}
{% endblock %}
